## Format

直接打轻量 tag 即可。

## Reference

tag: use `vX.Y.Z`, for example `v0.1.0`, `v1.2.3`
command: create a lightweight tag only
changelog: before tagging, convert `[Unreleased]` to `[X.Y.Z] - <YYYY-MM-DD HH:mm>` without the `v` prefix

## Example

```bash
git tag v1.2.3
```
